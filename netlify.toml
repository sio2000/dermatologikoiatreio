[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  external_node_modules = ["@netlify/blobs"]
  # Μόνο data αρχεία στο zip· ο υπόλοιπος backend κώδικας είναι μέσα στο bundled `api`.
  included_files = ["backend/data/**"]

[functions.environment]
  USE_BLOB_STORAGE = "true"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
  force = true

[[redirects]]
  from = "/health"
  to = "/.netlify/functions/api"
  status = 200
  force = true

# SPA fallback (μετά τα API redirects)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
